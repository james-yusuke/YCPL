import "std/fmt" as fmt

fn main() {
    match := 1
    or := 2
    go := match + or
    later := go + 4
    select := later + 5
    interface := select + 6
    is := interface + 7
    importas := is + 8
    mut := importas + 9

    fmt.println(mut)
}
