import "std/fmt" as fmt

fn main() {
    const label: string := "ok"
    name: string := none

    if name == none {
        fmt.println(label)
    }

    name = "set"

    if name != none {
        fmt.println(name)
    }
}
